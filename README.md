
  # Add Interactions and Animations

  This is a code bundle for Add Interactions and Animations. The original project is available at https://www.figma.com/design/o0qeitNAJQtVNA9ZCJaFL6/Add-Interactions-and-Animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  